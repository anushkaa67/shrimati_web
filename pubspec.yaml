name: shrimati_setu_guardian_dashboard
description: Guardian web dashboard for Shrimati Setu
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.4.0
  firebase_auth: ^5.2.0
  cloud_firestore: ^5.4.0
  firebase_storage: 12.4.10

  google_fonts: ^6.2.1
  url_launcher: ^6.1.15
  google_maps_flutter: ^2.17.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
